# Local Development Setup Guide

## Prerequisites

Before you begin, make sure you have:
- **Node.js** (version 18 or higher)
- **npm** or **yarn** package manager
- **Git** for cloning the repository
- **Supabase account** with your project already set up

## Step 1: Clone and Install

\`\`\`bash
# Clone the repository
git clone <your-repo-url>
cd nexttimetrackermasterbackup

# Install dependencies
npm install
# or
yarn install
\`\`\`

## Step 2: Environment Variables Setup

Create a `.env.local` file in the root directory:

\`\`\`bash
# Create environment file
touch .env.local
\`\`\`

Add the following environment variables to `.env.local`:

\`\`\`env
# Supabase Configuration (Required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Site Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Database URLs (Auto-generated by Supabase)
POSTGRES_URL=postgresql://postgres:[password]@db.[project-id].supabase.co:5432/postgres
POSTGRES_PRISMA_URL=postgresql://postgres:[password]@db.[project-id].supabase.co:5432/postgres?pgbouncer=true&connect_timeout=15
POSTGRES_URL_NON_POOLING=postgresql://postgres:[password]@db.[project-id].supabase.co:5432/postgres
POSTGRES_USER=postgres
POSTGRES_HOST=db.[project-id].supabase.co
POSTGRES_PASSWORD=your-db-password
POSTGRES_DATABASE=postgres

# JWT Configuration
SUPABASE_JWT_SECRET=your-jwt-secret
SUPABASE_ANON_KEY=your-anon-key-here
\`\`\`

## Step 3: Get Supabase Keys

### From Supabase Dashboard:

1. **Go to your Supabase project**: https://supabase.com/dashboard
2. **Navigate to Settings > API**
3. **Copy the following values**:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon/public key** → `NEXT_PUBLIC_SUPABASE_ANON_KEY` and `SUPABASE_ANON_KEY`
   - **service_role key** → `SUPABASE_SERVICE_ROLE_KEY`

### From Supabase Settings > Database:

4. **Navigate to Settings > Database**
5. **Copy Connection String** → Use for `POSTGRES_URL` (replace [password] with your actual password)

### JWT Secret:

6. **Navigate to Settings > API > JWT Settings**
7. **Copy JWT Secret** → `SUPABASE_JWT_SECRET`

## Step 4: Configure Supabase Auth

### Set Redirect URLs in Supabase:

1. **Go to Authentication > URL Configuration**
2. **Add these URLs**:
   \`\`\`
   Site URL: http://localhost:3000
   Redirect URLs:
   - http://localhost:3000/auth/callback
   - http://localhost:3000/auth/login
   - http://localhost:3000/dashboard/employee
   - http://localhost:3000/dashboard/organization
   \`\`\`

## Step 5: Database Setup

Run the database setup scripts in order:

\`\`\`bash
# Option 1: Run the complete setup script (recommended)
# Execute in Supabase SQL Editor:
scripts/00-complete-fresh-setup.sql

# Option 2: Run individual scripts if needed
# Execute these in Supabase SQL Editor in order:
scripts/32-add-missing-columns-safe.sql
scripts/33-check-time-entries-structure.sql
\`\`\`

## Step 6: Start Development Server

\`\`\`bash
# Start the development server
npm run dev
# or
yarn dev
\`\`\`

The application will be available at: **http://localhost:3000**

## Step 7: Test the Application

### Create Test Accounts:

1. **Visit**: http://localhost:3000/auth/signup
2. **Create an organization account**
3. **Create an employee account**
4. **Test login functionality**

### Test Features:

- ✅ **Authentication** (login/logout)
- ✅ **Organization Dashboard** (create projects, manage employees)
- ✅ **Employee Dashboard** (time tracking, task management)
- ✅ **Time Tracking** (start, pause, resume, complete tasks)

## Troubleshooting

### Common Issues:

#### 1. "Auth session missing" error:
\`\`\`bash
# Clear browser data and cookies
# Or visit: http://localhost:3000/logout
\`\`\`

#### 2. Database connection errors:
\`\`\`bash
# Verify your POSTGRES_URL is correct
# Check Supabase project is not paused
\`\`\`

#### 3. Environment variables not loading:
\`\`\`bash
# Restart the development server
npm run dev
\`\`\`

#### 4. RLS (Row Level Security) errors:
\`\`\`bash
# Run the database setup scripts
# Check user permissions in Supabase
\`\`\`

### Debug Endpoints:

- **Auth Debug**: http://localhost:3000/debug/auth
- **API Documentation**: http://localhost:3000/docs (after installing Swagger)

## Production Deployment

When ready to deploy:

1. **Update environment variables** for production
2. **Update Supabase redirect URLs** for your production domain
3. **Deploy to Vercel/Netlify** or your preferred platform

## Support

If you encounter issues:
1. Check the browser console for errors
2. Check the terminal/server logs
3. Verify all environment variables are set correctly
4. Ensure Supabase project is active and accessible
